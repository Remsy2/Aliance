# Aliance Production - Исправленная версия

## 📋 Описание

Это исправленная версия сайта Aliance Production с устраненными критическими и некритическими ошибками.

## 🔧 Что было исправлено

### Критические ошибки:
- ✅ Синтаксическая ошибка в footer.php
- ✅ Неопределенная переменная в main.js
- ✅ Незащищенные учетные данные API Telegram

### Серьезные ошибки:
- ✅ Дублирование ID элементов
- ✅ Отсутствие валидации на сервере
- ✅ Ненадежный метод отправки в Telegram

### Средние ошибки:
- ✅ Дублирование модальных окон
- ✅ Дублирование секции CTA
- ✅ Улучшена обработка ошибок в JavaScript

Подробный отчет смотрите в файле `ERRORS_REPORT.md`

## 📦 Установка

### 1. Замените файлы

Замените следующие файлы на новые версии:

```
footer.php       → Новая версия с исправлениями
header-page.php  → Новая версия без дубликатов
handler.php      → Полностью переработанная версия
main.js          → Исправленная версия
```

### 2. Настройте конфигурацию

**Способ 1: Используя config.php**

1. Скопируйте `config.example.php` в `config.php`:
```bash
cp config.example.php config.php
```

2. Откройте `config.php` и заполните ваши данные:
```php
define('TELEGRAM_BOT_TOKEN', 'ваш_токен_бота');
define('TELEGRAM_CHAT_ID', 'ваш_chat_id');
```

3. Обновите `handler.php`, добавив в начало:
```php
require_once 'config.php';
$token = TELEGRAM_BOT_TOKEN;
$chat_id = TELEGRAM_CHAT_ID;
```

**Способ 2: Оставить как есть (не рекомендуется)**

Если не хотите использовать config.php, оставьте текущую версию handler.php, но обязательно:
- Добавьте файл в .gitignore
- Никогда не публикуйте его в открытом репозитории

### 3. Добавьте .gitignore

Скопируйте файл `.gitignore` в корень проекта:
```bash
cp .gitignore /path/to/your/project/
```

### 4. Проверьте права доступа

Убедитесь, что файл handler.php имеет права на выполнение:
```bash
chmod 644 handler.php
```

## 🧪 Тестирование

После установки протестируйте:

### 1. Проверка синтаксиса PHP
```bash
php -l footer.php
php -l header-page.php
php -l handler.php
```

### 2. Проверка работы форм

1. Откройте сайт в браузере
2. Заполните форму обратной связи
3. Отправьте форму
4. Проверьте:
   - ✅ Форма отправляется без ошибок
   - ✅ Появляется модальное окно с благодарностью
   - ✅ Сообщение приходит в Telegram

### 3. Проверка валидации

Попробуйте отправить форму с:
- Пустым именем - должна показаться ошибка
- Коротким именем (1 символ) - должна показаться ошибка
- Неполным телефоном - должна показаться ошибка
- Корректными данными - форма должна отправиться

### 4. Проверка модальных окон

1. Нажмите "Получить консультацию" в шапке сайта
2. Проверьте открытие модального окна
3. Закройте окно кликом вне области
4. Откройте снова и закройте клавишей Escape

### 5. Проверка маски телефона

1. Начните вводить номер телефона
2. Маска должна автоматически форматировать: +7 (999) 999-99-99
3. Проверьте корректность позиции курсора при редактировании

## 📱 Мобильная версия

Протестируйте сайт на мобильных устройствах:
- Открытие/закрытие мобильного меню
- Отправку форм
- Работу модальных окон

## 🛠️ Требования

- PHP 7.0 или выше
- cURL extension для PHP
- JavaScript включен в браузере
- Swiper.js и JustValidate подключены (уже есть в проекте)

## 📁 Структура файлов

```
project/
├── index.php
├── about.php
├── contracts.php
├── trademarks.php
├── header-page.php      ← Обновлен
├── footer.php           ← Обновлен
├── handler.php          ← Обновлен
├── config.php           ← Новый файл (создать)
├── config.example.php   ← Новый файл (пример)
├── .gitignore           ← Новый файл
├── js/
│   ├── main.js          ← Обновлен
│   ├── swiper-bundle.min.js
│   └── just-validate.production.min.js
└── css/
    └── style.css
```

## 🔒 Безопасность

### Важные рекомендации:

1. **Никогда не коммитьте config.php в репозиторий**
   - Используйте .gitignore
   - Храните чувствительные данные отдельно

2. **На продакшене отключите отображение ошибок**
   ```php
   error_reporting(E_ALL);
   ini_set('display_errors', 0);
   ini_set('log_errors', 1);
   ```

3. **Регулярно обновляйте токен Telegram Bot**
   - Через @BotFather выполните /revoke
   - Обновите токен в config.php

4. **Добавьте HTTPS**
   - Telegram Bot API требует HTTPS для вебхуков
   - Используйте Let's Encrypt для бесплатного SSL

5. **Ограничьте частоту запросов**
   - Текущая версия handler.php готова к добавлению rate limiting
   - Раскомментируйте соответствующий код

## 🐛 Известные проблемы

Нет известных проблем. Все критические и серьезные ошибки исправлены.

## 📞 Поддержка

При возникновении проблем:

1. Проверьте файл `ERRORS_REPORT.md` - возможно, ответ там
2. Проверьте логи PHP: `/var/log/php_errors.log`
3. Проверьте логи веб-сервера: `/var/log/apache2/error.log` или `/var/log/nginx/error.log`
4. Проверьте консоль браузера (F12) на наличие JavaScript ошибок

## 📝 Changelog

### Версия 2.0 (04.02.2026)

#### Исправлено:
- Критическая синтаксическая ошибка в footer.php
- Ошибка неопределенной переменной в main.js
- Дублирование ID элементов в формах
- Отсутствие валидации данных на сервере
- Ненадежный метод отправки в Telegram API

#### Добавлено:
- Полная валидация на стороне сервера
- Использование cURL для отправки в Telegram
- Обработка ошибок и логирование
- Конфигурационный файл для безопасного хранения данных
- .gitignore для защиты чувствительных данных
- Улучшенное форматирование сообщений в Telegram

#### Улучшено:
- Обработка модальных окон
- Валидация форм на клиенте
- Маска ввода телефона
- Обработка ошибок в JavaScript
- Структура кода и читаемость

## ⚡ Быстрый старт

1. Замените 4 файла: `footer.php`, `header-page.php`, `handler.php`, `main.js`
2. Создайте `config.php` из `config.example.php`
3. Заполните данные Telegram Bot
4. Обновите handler.php (добавьте require_once 'config.php')
5. Скопируйте `.gitignore` в корень проекта
6. Протестируйте формы

Готово! ✨

## 📄 Лицензия

© 2022 Aliance Production. Все права защищены.
